namespace Appliance.Enums
{
    /// <summary>
    /// Relay components with Wiring Pi (wPi) pins for GPIO Header
    /// </summary>
    public enum Relay
    {
        LightsGarden = 29,
        AlarmStrobe = 27,
        AlarmSiren = 24,
        GarageRemoteButton = 7
    }
}
