namespace Appliance.Enums
{
    /// <summary>
    /// Sensor components with Wiring Pi (wPi) pins for GPIO Header
    /// </summary>
    public enum Sensor
    {
        OutdoorMotion = 0,
        OutdoorTamper = 1,
        GarageDoor = 11,

        FrontDoorMotion = -1
    }
}
